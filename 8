1.
int main()
{
	setlocale(LC_ALL, "RU");
 
    int a, b;
    cout << "Введите значения a и b: " << endl;
    cin >> a >> b;
    cout << "GCD of " << a << " и " << b << " is " << gcd(a, b);
    return 0;
}

   
